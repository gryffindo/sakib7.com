This is the reactified version of https://alhasan.herokuapp.com.

This is the completed versions of https://sakib7.com

If you want to run this application in your local machine, then simply clone this repository and then start with this command `yarn add` (If using npm then `npm i`). Then to run this in your browser run this `yarn start` or `npm start`.
